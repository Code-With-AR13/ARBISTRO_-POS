@model IEnumerable<ARBISTO_POS.Models.Items>

@{
    ViewData["Title"] = "Items";
}
<main class="nxl-container-fluid">
    <div class="nxl-content">
        <!-- Page Header -->
        <div class="page-header">
            <div class="page-header-left d-flex align-items-center">
                <div class="page-header-title">
                    <h5 class="m-b-10">@ViewData["Title"]</h5>
                </div>
                <ul class="breadcrumb">
                    <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Home</a></li>
                    <li class="breadcrumb-item active">@ViewData["Title"]</li>
                </ul>
            </div>
            <div class="page-header-right ms-auto">
                <div class="d-flex align-items-center gap-2 page-header-right-items-wrapper">
                    <a asp-controller="Items" asp-action="Create" class="btn btn-primary">
                        <i class="feather-plus me-2"></i>
                        <span>New Item</span>
                    </a>
                </div>
            </div>
        </div>

        @if (TempData["SuccessMessage"] != null)
        {
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <i class="feather-check-circle me-2"></i>
                <strong>Success!</strong> @TempData["SuccessMessage"]
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        }

        <div class="main-content">
            <div class="row">
                <div class="col-lg-12">
                    <div class="card stretch stretch-full">
                        <div class="card-header">
                            <h5 class="card-title mb-0">
                                <i class="feather-shopping-bag me-2"></i>All Items
                            </h5>
                        </div>
                        <div class="card-body p-0">
                            <div class="table-responsive">
                                <table class="table table-hover align-middle dataTable">
                                    <thead class="table-light">
                                        <tr>
                                            <th>Item</th>
                                            <th>Category</th>
                                            <th>Ingredient</th>
                                            <th>Cost</th>
                                            <th>Price</th>
                                            <th class="text-center" style="width: 120px;">Actions</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @if (Model != null && Model.Any())
                                        {
                                            @foreach (var item in Model)
                                            {
                                                <tr>
                                                    <td>
                                                        <div class="d-flex align-items-center">
                                                            <div class="me-3">
                                                                @if (!string.IsNullOrEmpty(item.CateImage))
                                                                {
                                                                    <img src="@item.CateImage"
                                                                         alt="@item.ItemName"
                                                                         class="rounded-circle border"
                                                                         style="width: 80px; height: 80px; object-fit: cover;"
                                                                         onerror="this.src='/assets/images/upload.jpg'">
                                                                }
                                                                else
                                                                {
                                                                    <img src="~/assets/images/upload.jpg"
                                                                         alt="No Image"
                                                                         class="rounded-circle border"
                                                                         style="width: 80px; height: 80px; object-fit: cover;">
                                                                }
                                                            </div>
                                                            <div class="d-flex flex-column">
                                                                <span class="fw-semibold">@item.ItemName</span>
                                                                @if (!string.IsNullOrEmpty(item.ItemDiscription))
                                                                {
                                                                    <span class="text-muted small">@item.ItemDiscription.Substring(0, Math.Min(50, item.ItemDiscription.Length))@(item.ItemDiscription.Length > 50 ? "..." : "")</span>
                                                                }
                                                            </div>
                                                        </div>
                                                    </td>
                                                    <td>
                                                        <span class="badge bg-light text-dark">@Html.DisplayFor(modelItem => item.FoodCategory.CateName)</span>
                                                    </td>
                                                    <td>
                                                        @if (item.ItemIngredients != null && item.ItemIngredients.Any())
                                                        {
                                                @string.Join(", ", item.ItemIngredients
                                                                                                .Select(ii => ii.Ingredient.Name))
                                                                                                }

                                                    </td>
                                                    <td>
                                                        <span class="text-muted">@item.ItemCost.00</span>
                                                    </td>
                                                    <td>
                                                        <strong>@item.ItemPrice.00</strong>
                                                    </td>
                                                    <td class="text-center">
                                                        <div class="d-inline-flex gap-1">                                                            
                                                            <a asp-action="Edit"
                                                               asp-route-id="@item.ItemId"
                                                               class="btn btn-sm btn-light"
                                                               title="Edit">
                                                                <i class="feather-edit-3"></i>
                                                            </a>

                                                            <!-- unique id per form -->
                                                            <form asp-action="Delete"
                                                                  asp-route-id="@item.ItemId"
                                                                  method="post"
                                                                  class="d-inline delete-form">
                                                                @Html.AntiForgeryToken()

                                                                <button type="button"
                                                                        class="btn btn-sm btn-light js-delete"
                                                                        title="Delete">
                                                                    <i class="feather-trash-2"></i>
                                                                </button>
                                                            </form>
                                                        </div>
                                                    </td>
                                                </tr>
                                            }
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</main>
@section Scripts {
    <script>
        $(document).ready(function () {

            $('.dataTable').DataTable();

            $('.js-delete').on('click', function (e) {
                e.preventDefault();

                const form = $(this).closest('form');

                Swal.fire({
                    title: 'Are you sure?',
                    text: 'This item will be permanently deleted.',
                    icon: 'warning',
                    showCancelButton: true,
                    confirmButtonColor: '#d33',
                    cancelButtonColor: '#6c757d',
                    confirmButtonText: 'Yes, delete it',
                    cancelButtonText: 'Cancel'
                }).then((result) => {
                    if (result.isConfirmed) {
                        form.submit(); // ✅ SIMPLE POST
                    }
                });
            });

            // auto hide success message
            setTimeout(function () {
                $('.alert-success').fadeOut('slow');
            }, 4000);
        });
    </script>
}


